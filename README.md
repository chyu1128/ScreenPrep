# Vibe Coded with OpenAI Codex

This application is **vibe coded**, meaning the vast majority of the application logic and implementation details were generated by **OpenAI Codex**. 

### Development Workflow
This application was developed with substantial assistance from OpenAI Codex. I designed the preprocessing workflow, requirements, and application behavior, while Codex was used to generate and refine much of the implementation. I reviewed, tested, and adjusted the resulting code.

[![Vibe Coded](https://shields.io)](https://github.com)


# ScreenPrep

A lightweight RDKit desktop GUI for cleaning raw data before proceeding the screening processes. It reads `ID`, `SMILES`, and `VALUE`, checks for missing values, validates each structure, removes salts , exclude duplicate structures, and optionally excludes PAINS matches.

The output can be previewed and modified manually before exporting into a screening-ready CSV with the same three columns.

## Setup

RDKit is most reliably installed from conda-forge:

```bash
conda create -n screenprep -c conda-forge python=3.11 rdkit
conda activate screenprep
python app.py
```

## Input format

```csv
ID,SMILES,VALUE
CPD-001,CC(=O)Oc1ccccc1C(=O)O,12.4
CPD-002,CN(C)CCOC(c1ccccc1)c1ccccc1.[Cl-],83.2
```

The exported CSV includes only retained compounds and preserves the header order `ID,SMILES,VALUE`. Duplicate detection compares the canonical, desalted SMILES; the first occurrence is retained.
