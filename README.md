# ScreenPrep

A lightweight RDKit desktop GUI for cleaning raw data before proceeding with screening processes. It reads `ID`, `SMILES`, and `VALUE`, checks for missing values, validates each structure, removes salts , exclude duplicate structures, and optionally excludes PAINS matches.

The output can be previewed and modified manually before exporting into a screening-ready CSV with the same three columns.

## Vibe Coded with OpenAI GPT-5.6 Terra

This application is **vibe coded**, meaning the vast majority of the application logic and implementation details were generated by **OpenAI GPT-5.6 Terra**. 

### Development Workflow
This application was developed with substantial assistance from OpenAI GPT-5.6 Terra. I designed the preprocessing workflow, requirements, and application behavior, while GPT-5.6 Terra was used to generate and refine much of the implementation. I reviewed, tested, and adjusted the resulting code.

## Setup

RDKit is most reliably installed from conda-forge:

```bash
conda create -n screenprep -c conda-forge python=3.11 rdkit
conda activate screenprep
python app.py
```

## Input format

```csv
ID,SMILES,VALUE
CPD-001,CC(=O)Oc1ccccc1C(=O)O,12.4
CPD-002,CN(C)CCOC(c1ccccc1)c1ccccc1.[Cl-],83.2
```

The exported CSV includes only retained compounds and preserves the header order `ID,SMILES,VALUE`. Duplicate detection compares the canonical, desalted SMILES; the first occurrence is retained.
